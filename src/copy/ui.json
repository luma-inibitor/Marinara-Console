{
  "_readme": "ui area copy. Empty until src/ui is migrated onto the catalog — see src/copy/index.ts for the entry shapes and design/copycheck.mjs for what is enforced. Keys beginning with an underscore are metadata and are never copy."
}
